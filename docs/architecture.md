# SpellFire Architecture

SpellFire is a playable browser-based multiplayer foundation: a Pixi.js/TypeScript client connects to a server-authoritative Go simulation over binary WebSockets. Account and character metadata is persisted in SQLite. All in-world visuals are built at runtime with Pixi geometry primitives; there are no bitmap assets in the play space.

This document describes what is implemented in version 0.1. [`game/design/`](game/design/README.md) is authoritative for game rules, and [`game/ui/`](game/ui/README.md) is authoritative for player-facing presentation.

## Repository layout

```text
proto/game.proto                    Canonical WebSocket schema
data/tuning/                        Versioned balance tables, read by both server and client
data/data.go                        Embeds the tables into the Go binary
server/cmd/spellfire/               Process entry point and static-client hosting
server/internal/api/                JSON account and character HTTP API
server/internal/auth/               Password hashing, opaque sessions, authentication
server/internal/config/             Environment configuration
server/internal/crafting/           Recipe resolution, costs, staff tiers, and component modifiers
server/internal/game/               Fixed-tick world, physics, combat, gunplay, AOI, rewind, spatial index, chunked terrain, outposts, rideables
server/internal/loadout/            Equipped-slot model, resolution, and validation
server/internal/worldfield/         Deterministic danger/biome/grade field and its coverage check
server/internal/model/              Persistent domain types
server/internal/protocol/           Protobuf wire codec
server/internal/store/              Persistence interface and SQLite implementation
server/internal/transport/          WebSocket lifecycle and origin enforcement
server/internal/tuning/             Tuning-table schema, loader, and validation
web/src/api.ts                      Typed account/character API client
web/src/tuning.ts                   Client-side view of the same tuning tables
web/src/net/                        Protobuf codec and reconnecting socket
web/src/game/crafting.ts            Client recipe resolution, costs, compatibility, and previews
web/src/game/loadout.ts             Client-side view of the slot model and unlock ledger
web/src/game/worldfield.ts          Bit-identical browser copy of the world field
web/src/game/outposts.ts            Browser mirror of the outpost safety/service overlay
web/src/game/prediction.ts          Local prediction and server reconciliation
web/src/game/view.ts                Procedural Pixi renderer and interpolation
web/src/main.ts                     Application state, inputs, HUD, and menus
web/src/style.css                   Responsive UI and touch-safe layout
```

Module dependencies point inward: transport calls authentication, persistence, and the game engine; the world simulation has no HTTP, WebSocket, database, or Pixi dependency. This keeps authoritative rules directly testable.

## Runtime topology

```text
Browser DOM UI ──JSON/HTTP──> account + character API ──> Store interface ──> SQLite
      │
      └─Pixi renderer ──binary Protobuf/WebSocket──> connection handler ──> game engine
                                                               │
                                                     authoritative World
```

The Go process serves the built SPA, API, WebSocket endpoint, and one shared world. SQLite was selected for the initial implementation because it has no external runtime dependency and gives deterministic integration tests. It is isolated behind `store.Store`; a PostgreSQL implementation can replace it without changing account, API, transport, or game logic. Horizontal world sharding is deliberately not claimed by this single-process foundation.

## Application and account flow

- `POST /api/auth/register` validates the email/password, hashes passwords with bcrypt, creates an account, and returns a new opaque session token plus the server-derived account view.
- `POST /api/auth/login` returns the same generic failure for unknown accounts and incorrect passwords, and returns the token plus account view on success.
- `POST /api/auth/logout` revokes only the presented session.
- `GET /api/account` returns the authenticated account email and `is_admin` role.
- `GET|POST /api/characters` lists or creates account-owned characters. Names are 3–20 characters, both GDD classes are accepted, and the initial account limit is four characters.
- `GET /api/version` returns `{time, commit}` describing how the running binary was built. The values are stamped at link time via `-ldflags -X spellfire/server/internal/build.{Time,Commit}` (the `make build` target and the Dockerfile set these; the Docker commit comes from the `BUILD_COMMIT` build arg). For an unstamped local `go build`/`go run` in the checkout, `build.Get` falls back to Go's embedded VCS metadata. The home screen fetches this and shows the build age, so a stale deployment is visible at a glance.
- The raw session token is shown only once to the client. SQLite stores a SHA-256 digest, so a database disclosure does not directly disclose live bearer tokens. The browser keeps the token in `sessionStorage`, preserving refreshes in the current tab without creating an indefinitely persistent browser credential.
- A WebSocket must send a Protobuf `JOIN` as its first binary message. The server authenticates its token and verifies character ownership before inserting the player into the world.
- A later connection for the same character replaces the active transport. The old connection cannot delete the replacement when it eventually closes.
- An account gets one body in the world at a time. `Engine.Join` refuses a join with `ErrAccountInWorld` when the account already occupies the world through a *different* character, and the socket answers with an error and closes. See [one body per account](#one-body-per-account).

Production deployment must terminate TLS in front of the process, rate-limit authentication endpoints, back up the database, and set an explicit trusted-origin policy if the frontend and backend are split across hostnames. Email verification and password recovery are not present in this rudimentary account implementation.

### Administrator authorization

`data/tuning/admins.json` is the server-side list of administrator account emails. Both configured values and stored account emails are trimmed and compared case-insensitively, matching the registration/login identity rule. The table is embedded, validated for malformed and duplicate normalized values, and takes effect on rebuild/restart. It is deliberately not imported into the browser bundle.

Authentication produces an `auth.Principal` containing the persisted account ID, email, and server-derived admin bit. The API recomputes that principal on every authenticated request by joining the opaque session to its account, so an existing session follows configuration changes after a restart. `api.withAdmin` is an opt-in boundary for privileged HTTP features: it distinguishes a missing/expired session (`401`) from a signed-in non-admin (`403`). The `is_admin` value returned to the client supports presentation only and is never accepted back as authority.

Developer mode is the first protected feature. Spawnability and form schemas live with each archetype in `data/tuning/entities.json`: fields name a stable `component.attribute` binding, spawn/edit scope, and number/text/select/position/rotation input contract. The client builds the catalog and forms from those rows, while the server parses the same data and validates every value. `game.adminAttributeRegistry` is the sole explicit adapter from stable bindings to today's embedded structs; it avoids reflection and can be retargeted to ECS component stores without changing tuning, HTTP, or the UI. `/api/admin/spawn` places an archetype — including a standing smoke field, so concealment can be looked through without a Gunslinger having to throw one — and `/api/admin/entity/inspect`, `/edit`, and `/delete` target any live entity selected from a snapshot. The legacy caller-only `/api/admin/attributes` route remains compatible through the same registry. Material-grant bounds live at `materials.admin_grant` and level-grant bounds at `progression.admin_grant`. Every command verifies administrator status and controlling-character ownership before crossing the engine seam; the world never receives an account token. Operational details are in [`administration.md`](administration.md).

`transform.position` is one canonical `[x,y]` value in the registry and API. The client renders it as adjacent unbounded number boxes plus a one-click world-position picker, while the server validates both coordinates against tuning and the entity's world extent. `transform.heading_degrees` is a rotation input and adapter: it rotates projectile velocity without changing speed and updates telegraph direction. Generic numeric HTML inputs omit `min`, `max`, and `step`; those constraints remain in tuning and are enforced by `validateAdminValue`.

## Persistence and migration

Two independent versions govern a save, and neither one is the tuning tables'.

`PRAGMA user_version` records how far the *database schema* has been migrated. `store.migrations` is an append-only list whose index+1 is the version it leaves behind; opening a database applies every migration it has not seen, each inside a transaction with its own version bump, so an interrupted upgrade is never recorded as complete. A database written by a newer build is refused rather than downgraded. A v0.1 database predates the version counter, reports 0, and migrates forward in place because migration 1 is written idempotently.

`characters.schema_version` records the *record* shape, which changes independently of the table layout. `model.Character.Migrate` carries an older row forward through sequential steps inside the store's scan path, so nothing above the store ever sees an older shape, and `SaveCharacterState` stamps the current version when the record is next written. Version 1 is the original name/class/level/xp record; version 2 adds saved world position, carried materials, and unlocked outposts; version 3 adds the last-seen stamp that decides whether that position is still honoured; version 4 adds the equipped loadout; version 5 adds the permanent unlock ledger. Neither counter is the tuning tables' `schema_version`, which reached 7 for the first developer catalog, 8 for slot/gadget data, 9 for progression/unlock levels, 10 for common entities and fixtures, 11 when admin schemas moved onto entity archetypes, 12 for vector-position and rotation admin inputs, 13 for gunplay and special ammunition, 14 for deployable fields and the developer level grant, 15 for recipe-resolved guns plus tiered mana-crystal/stave staff assemblies, 16 for shield durability, 17 for the Mage kit's delivery shapes, pulsing fields, and crystal area/element modifiers, 18 for combat roles and damage-band enforcement, 19 for terrain vision/shadow attributes, 20 for keystone removal, 21 for the world's scale substrate — chunk size and the chunked terrain scatter that replaced the fixed tree count — 22 for the world field: the biome lattice, the convex reward curve, the coverage rule, biome summaries and palettes, and the material kinds the two new biome-gated sources added, 23 for the per-biome terrain archetypes and the ridge-belt macro structure, and 24 for outpost safety and rideables: the outpost row's bubble/discovery radii and service set, the `rideables.json` table, and the session's exit-invulnerability and mount-lockout windows. A table-shape change needs no character migration because these instances are derived/in-memory rather than persisted. A record from a newer build is an error, because writing it back would truncate fields this build cannot see.

A character keeps its world position, carried raw materials, unlocked outposts, and equipped loadout across a disconnect: position in nullable scalar columns (unplaced and placed-at-the-origin are different states), materials, outposts, and the loadout as JSON on the same row, since the access pattern is always whole-character. Crafted items live in `crafted_items` as a blueprint ID plus a slot → component ID map — references only, never a stat snapshot, so a balance edit retunes every owned item in place. Crafting itself arrives in Phase 2.3; the record contract does not wait for it.

### Logout, linger, and recall

Dropping the connection does not remove the body. `Engine.Leave` unregisters the client and starts a logout window — `session.logout_linger_seconds`, 10 as shipped — during which the body stands in the world: it takes damage and can be killed, but it cannot move, dash, or fire, because `stepPlayer` treats a lingering player the way it treats a dead one. Snapshots flag it as `lingering`, and the client lowers its opacity and marks the plate “offline,” so attackers do not mistake it for an active player. Disconnecting is therefore not an escape from a fight. Reconnecting inside the window resumes that same body wherever the fight has since moved it, which doubles as the reconnect path. Resuming clears the body's input state: the input sequence numbers the connection, not the character, so a fresh client counting from one again would otherwise have every input rejected as stale by `ApplyInput` and every predicted input discarded against a stale acknowledgement. Only a living body is resumed — a body killed inside its window is dropped on reconnect and the character enters at the hub, so being killed while logged out costs the position exactly as it would had the window closed first and the unplaced save decided the entry. When the window closes, the tick loop reaps the body, saves its final position, and removes it.

### One body per account

An account may have only one character in the world. `World` indexes bodies by account — `occupants`, maintained by `AddPlayer` and `RemovePlayer` — and `Engine.Join` refuses with `ErrAccountInWorld` when that index already holds a character other than the one joining. Rejoining as the *same* character is untouched: that is the reconnect and replacement path above.

The occupancy follows the body, not the connection, so it outlasts a disconnect for the length of the logout window. This is deliberate. The alternative — evicting the other character on demand — would hand every player a combat-log escape: drop the connection mid-fight, join a second character, and the body that was supposed to stand there for ten seconds disappears. Refusing the join instead makes the linger window the only way out, and the cost is bounded by that same window: after a normal exit, the second character can enter as soon as the first body is reaped. The refusal reaches the player as the transport's `SERVER_ERROR`, which the client shows on its terminal connection overlay with the return-home action.

Characters with no account ID are never indexed, so the simulation tests, which build characters without one, do not contend for a single shared slot.

A saved position is honoured only while it is fresh. Every save is stamped, and on join a stamp older than `session.position_expiry_seconds` — 30 minutes as shipped — stops being trusted: the character is recalled to the safe fixture nearest to where it logged out, choosing between its unlocked outposts and the central hub. `data/tuning/outposts.json` holds those positions, and since Phase 3.3 it carries rows across the Fringe and Frontier, so a recall now lands on real geography rather than always on the hub. It is the same `nearestUnlockedOutpost` rule a death resolves through — see [outposts, safety, and travel](#outposts-safety-and-travel) — so logging out can never reach somewhere a death would not have. An unstamped position (a record migrated from schema version 2) counts as expired, because there is no way to tell how old it is. A position is also refused when the world can no longer accept it — outside the rim, or inside cover that did not exist when it was written — and falls back to the deterministic hub spawn ring.

The engine owns save timing through the narrow `game.Persister` interface, so the simulation still has no database dependency. Writes leave the tick loop through a buffered channel into a single writer goroutine, so a slow database can never delay a tick; an overflowing queue writes on its own goroutine rather than dropping the save. Saves happen when a logout window closes, every 15 seconds for everyone present, and once more for everyone present at shutdown, lingering bodies included. A connection that has already been replaced touches nothing, or it would strand or overwrite the live session. A dead player is saved unplaced and re-enters at the hub; a live respawn instead relocates to the nearest unlocked outpost, and Phase 4.2 still owns the ~5 s timer.

Because a save stores references, content can only be withdrawn, never deleted. `data/tuning/retired.json` maps every retired ID to a live replacement or a material refund, validated for dangling and circular chains at load, and `Tables.Resolve` follows it. Carried materials pass through it on join, so a retired material arrives as its replacement or its refund instead of vanishing. The client does not bundle this table: retirement resolves persisted references, and the client holds none.

## Simulation and world rules

`game.World` owns all mutable gameplay state and advances on a fixed 60 Hz tick. `game.Engine` serializes joins, leaves, inputs, respawns, and simulation steps with a mutex. The default world is a 22,500-unit-radius contiguous circle with a 450-unit central service-safe hub, a PvP-protected fringe out to 4,500 units, a Frontier to 15,750, and deterministic procedural terrain generated per chunk outside the hub. See [scale, the spatial index, and chunk residency](#scale-the-spatial-index-and-chunk-residency) for how a world that size is held.

Implemented authoritative rules include:

- normalized eight-direction movement at a capped speed;
- a timed dash that carries the player at high speed along the direction locked in at the press (movement input, or aim when standing still) for a whole number of ticks, overriding steering for its duration and colliding normally;
- circular player/world bounds plus circle and axis-aligned-box world-item collision, resolved independently on each movement axis;
- server-authoritative aim, universal dash, dash cooldown, health, Mage mana/regen, Gunslinger magazine/reload, ability cadence and cooldown, projectile lifetime, damage, death, and central-hub respawn;
- one ability path for every deliberate action, and one status-effect layer applied through it (see [abilities and effects](#abilities-and-effects));
- effective-damage attribution and most-contribution kill credit through a bounded combat log;
- the nine settled gun categories, separated by handling rather than by damage: magazine, reload, range, recoil pattern, spread, weight class, scope, pellets, and blast (see [gunplay](#gunplay));
- the settled 5 × 4 spell grid, delivered through placement, pulsing fields, blink, chain, homing, dispel, and player-authored walls (see [spells and the Mage kit](#spells-and-the-mage-kit));
- approximately three-second uncontested raw TTK from the sustained, burst, and heavy-burst tuning bands; each band owns both damage and cadence, while item rarity multiplies only its damage anchor;
- no PvP damage inside the protected hub/fringe, while projectiles may still resolve visibly;
- deterministic ordering for player and projectile processing and deterministic tree generation.

### Common entity and collision model

Every materialized simulation object embeds `game.Entity`: players, projectiles, telegraphs, procedural trees, and authored walls therefore share ID/kind, position/velocity, mass, current/max health, alive state, and a list of collision objects. This is a typed data base rather than an interface hierarchy. `Player`, `Projectile`, and `Telegraph` keep only their family-specific state beside it, while fixed and procedural terrain live directly as entities in the world's [spatial index](#scale-the-spatial-index-and-chunk-residency). Hot movement and projectile loops query that index rather than dispatching through an interface; the same layout can later be split into ECS component columns without changing gameplay semantics.

`data/tuning/entities.json` owns the defaults. A runtime entity receives a copy of its archetype, so mutable health or geometry never changes the shared row. `EntityOverrides` uses pointer-valued scalar fields and an explicit collision-list replacement: zero is a valid override, and an empty collision list is distinct from no override. The shipped tree archetype has mass `-1`, 500 health, and one circular collision object; the procedural generator overrides that circle's radius per instance. The shipped wall has mass and health `-1` and one 96 × 96 box, and `world.fixtures` places `wall-00` at `(2400, 0)`, outside the hub. Negative mass means immovable; negative health means undestroyable. Projectiles damage destroyable terrain through the same `Entity.TakeDamage` primitive used beneath player damage, and a destroyed tree immediately leaves both authoritative collision and AOI snapshots.

Collision objects are local primitives with an optional offset. Circle and axis-aligned box are implemented. The server sends AOI-filtered collision components separately from visual entity state so client prediction can keep a compact hot set. Each snapshot replaces that set rather than merging it, which removes a destroyed tree or an exited-AOI fixture immediately. Multiple objects per entity are supported by unique component IDs even though the shipped archetypes currently use at most one.

Every family promotes `Entity.Delete(time.Time)` and satisfies the small `Deletable` lifecycle interface. Forced deletion immediately leaves action and collision, remains in snapshots for a 350 ms normalized fade plus a short snapshot-delivery grace, and is then collected by its owning store. Connected players deliberately remain as ordinary dead bodies after the fade so the existing death overlay and respawn path stay valid; respawn clears deletion state. Disposable admin players and non-player entities are physically reaped. The wire carries `deleting` and `delete_progress`, allowing the frontend to fade deliberately instead of guessing whether snapshot absence means removal or AOI exit.

### Scale, the spatial index, and chunk residency

The world is a circle of radius 22,500. At MMO-plausible collider density that is many thousands of world items, so neither of the two things the pre-3.0 world did — hold every item in one flat slice, and walk that slice from every collision test, projectile step, occluder collection, and per-viewer snapshot — survives the scale. Both are replaced here, and this is the substrate the rest of Phase 3 builds on.

**One uniform grid answers every broad-phase question.** `game/grid.go` buckets entities into square cells whose edge is `world.chunk_size` — 1,200 as shipped, the AOI half-extent, so a viewer's interest square always falls inside a 3 × 3 neighbourhood. Terrain, players, projectiles, telegraphs, and fields each get their own cell map, so no query filters a mixed bucket by type. A member is bucketed by its position alone and never smeared across the cells it overlaps; queries widen their cell range by the largest reach the bucket has seen instead, which keeps membership single-valued and iteration duplicate-free. `Deployable` declares `QueryExtent` because a field is an area rather than a body; everything else answers with its collision geometry. Movement re-buckets only when a body crosses a cell edge, which at this cell size a running player needs seconds to do.

Map iteration order is not deterministic, so nothing is allowed to depend on it: queries visit cells in a fixed order, and every result a decision is made from is ordered by ID before it is used. Two consequences are worth naming. `advanceProjectile` now stops a round at the *nearest* blocker along its step rather than the first one a scan happened to reach, since a step can cross two colliders. A rewound resolve widens its swept query by how far a body can travel inside the rewind window, because the position that matters is where the body *was*.

**Terrain is chunked, so the world is never fully resident.** `game/chunk.go` materialises one chunk — one index cell — from `(world seed, chunk coordinate)` when a body comes within the load reach, and drops it again past the keep reach. The load reach is the AOI half-extent plus the widest scope bonus in the tables plus half a chunk, so a scoped player never looks at ground that has not been generated; the keep reach adds a whole chunk of hysteresis so walking a chunk edge does not thrash the generator. Nothing about a chunk is stored: residency is a cache, and the seed is the world.

Placement is a **jittered lattice**, not rejection sampling. Rejection sampling has to see everything already placed, which a chunk cannot — its neighbour may not exist yet, and generating it later must not change the answer. A lattice needs to see nothing: one candidate site per `terrain.cell` (400 units), a per-site draw from the seed and the site's own lattice coordinates deciding whether it carries an item and how wide it is, and jitter clamped to exactly what `terrain.spacing` and the archetype's widest radius leave room for. Two items therefore cannot overlap, in one chunk or across a chunk edge, whichever order the chunks were generated in — which is what `TestGeneratedTerrainNeverOverlapsAcrossChunkEdges` holds. Authored fixtures are the only thing generation defers to, and it reads them from the table rather than from the world, so that deference is independent of what is resident.

**The lifecycle may not break contracts the rest of the simulation relies on.** Two rules do that work. A chunk holding an item the world has *changed* — damaged, or inside its `Entity.Delete` fade — is **pinned**, because evicting and regenerating it would repair the damage and because both the fade and the rewind window's `Entity.presentAt` history assume an item outlives its own removal. Destruction leaves a **scar**: the felled site is remembered by ID and never regenerated, so a chunk that comes back is the world the players left rather than the world the seed describes. Runtime-authored terrain — authored fixtures, Mage walls, developer spawns — is not chunked at all but lives in a resident set, which satisfies the same contracts by construction: a wall cannot be evicted out from under its own lifetime because eviction never sees it. Chunks load before they can be seen and are dropped only well outside interest, so the rewind window never spans a residency change.

**The scale is data, and every radius derived from it moved with it.** `world.json` carries radius 22,500 and bands at hub 450, Fringe 4,500, Frontier 15,750, and Deadlands 22,500 — the shipped 0.70 Frontier fraction preserved, with the hub now a settlement footprint rather than a scaled-up spawn ring. The spawn ring, terrain margins, developer-mode position bounds, and the client's world-ring rendering moved with them; the AOI half-extent deliberately did not, because it is a camera property rather than a world one — which is why the first ridge belt now crowds closer to the fixed-size camera than it did at the old scale. The renderer draws the rim as the arc facing the camera rather than a full circle, since a tessellated 22,500-unit circle is thousands of vertices rebuilt every frame for a few visible pixels. `TestRadiusDerivedConstantsHoldAtTheNewScale` keeps the set consistent.

### The world field

`server/internal/worldfield` answers what the world *is* at a position: which danger band it sits in, which biome covers it, what material grade the ground yields, and which region that ground belongs to. It holds no world state and imports no table — every input arrives as a plain `Params` value — so the simulation, the loader's own coverage check, and the browser all reach one answer from one implementation. `web/src/game/worldfield.ts` is a hand-written mirror of it, and `testdata/worldfield.json` is the fixture both suites check themselves against, so the two cannot drift silently.

**Danger is now a lookup rather than a radius comparison.** `World.DangerAt`, `World.Protected`, and `World.Safe` are the simulation's whole zone vocabulary: the loadout lock, the crafting gate, the Mage wall's placement rule, and both ends of PvP protection ask one of them. Nothing compares a distance against a radius of its own any more, which is what lets [Phase 3.3](../TODO.md) re-resolve safety against the nearest outpost by rebuilding the field instead of by finding every comparison. `Tuning.SafeRadius` and `PvPRadius` survive only as the scalars terrain margins and the spawn ring are laid out against.

**Biomes are radially compressed, noise-warped Voronoi regions.** A position is first pulled toward `field.radial_reference` by a fixed 3/4 power, then bent by two octaves of value noise, and only then bucketed against a jittered lattice of `field.region_cell`. The compression is the whole reason [biome and grade are independent axes](game/design/world.md#biomes-type--grade): it turns a region from a blob at one radius into a swathe that narrows toward the hub and widens toward the rim, so every element is reachable from every danger band rather than only from the ring it happened to land on. The nearest site of a *different* biome supplies a border blend — 0 on the border, 1 deep inside — which is what the renderer cross-fades an ambient palette across, so a crossing reads as a coastline rather than a seam.

**Grade rises with radius on a convex curve.** `field.grade_curve.points` is a piecewise-linear reward curve over the fraction of the world radius, and the loader requires each segment to be at least as steep as the one before it. That convexity is the incentive shape [world.md](game/design/world.md#radial-danger) asks for: reward rises slowly across the Fringe and steeply toward the Deadlands, so the middle bands are a route rather than the best farm. `thresholds` names the grade each stretch yields — climbing one rarity tier at a time from Common and stopping at Rare, because Signature is a boss drop rather than something the ground holds — and each danger band's declared `material_grade` is cross-checked against the curve at its own outer edge, so the HUD and the ground can never disagree. The curve's continuous value survives as `Grade.Richness`, which is the scalar Phase 4.1's harvest yields scale with: within one grade, the outer Frontier is still worth substantially more than the inner Frontier.

**Everything is written to be reproducible in two languages.** Every hash is 32-bit integer arithmetic, which JavaScript reproduces exactly through `Math.imul` and the unsigned shift operators; a 64-bit hash would have needed BigInt on the other side. Every float operation is `+`, `-`, `*`, `/`, or `sqrt`, all of which IEEE-754 specifies to the bit. `math.Pow` and JavaScript's `Math.pow` are *not* required to agree, which is why the radial compression is two square roots rather than an exponent and why the reward curve is piecewise linear rather than a power. Nothing in the field uses trigonometry, and the coverage sampler clips a square lattice to the disc rather than sweeping by angle for the same reason.

**A bad seed is refused at load, not shipped and patched around.** The biome map is generated, so nothing guarantees by construction that a Fringe player can reach Frost material. `validateField` therefore samples the field across every band and refuses a seed that leaves a biome absent from one, or whose share falls below `field.coverage.minimum_share`. The hub is exempt — it yields nothing, so its mix decides nothing. This is the enforced form of "geography never hard-locks a build"; the shipped seed clears its own 6% floor with a 15% worst case, and `TestShippedSeedCoversEveryElementInEveryBand` fails a re-roll that only just squeaks past.

**The economy half is `Tables.MaterialsAt`.** Universal kinds — structural stock, stave wood, and reagents — are available in every biome, which is what keeps geography from hard-locking a build. Biome-gated kinds are not: `aligned` stock is taken from what lives in a region and `growth` stock is cut or quarried from its ground, so a biome is worth travelling to whether or not anything is hunting there. Grade is a **ceiling** rather than an equality, so walking outward adds options instead of trading them. `World.MaterialsAt` resolves that through the field, which is the form harvesting will ask it in; nothing produces a material yet, but the rule that decides what a node could hold is the field's rather than the node's.

### Terrain, friction, and traversal

`game/terrain.go` and the two-layer generator in `game/chunk.go` are what make a radius-22,500 world a journey roughly twice the length of the straight walk it forbids. Both layers materialise per chunk from `(seed, chunk_coord)` and read the [world field](#the-world-field) for the biome underfoot, so the ground reads as its region before the HUD names it, and nothing about a chunk is stored: the same coordinate always produces the same terrain however chunks load and evict.

**The scatter layer is avoidable landmarks.** Each biome in `world.json` declares a `scatter` list — trees and boulders in Stonereach, cinder spires in the Emberlands, ice blocks in the Rimeshelf, and so on — whose `fill` values are absolute probabilities a site carries that archetype, summing to at most one with the remainder bare ground. Placement is the same jittered lattice trees used, and the jitter clamp uses the *widest* archetype any biome could place, so a site can hold anything without a neighbour ever overlapping it. `TestGeneratedTerrainNeverOverlapsAcrossChunkEdges` still holds for scatter; belts are the deliberate exception.

**The belt layer is the macro structure.** `belts.radii` places concentric impassable ridge belts, each an annulus `thickness` thick whose centre wobbles with the angle so the ridge is not a drawn circle. A belt is filled on a fine lattice (`belts.cell`, deliberately smaller than twice the barrier archetype's radius) with *overlapping, un-jittered* formations, so it is a solid, un-dashable mass rather than a passable picket of boulders — the barrier archetype is the biome's own (`boulder-ridge`, `lava-flow`, `ice-shelf`, `fulgurite`, `mirror-monolith`), so a ridge crossing the Emberlands is lava and one crossing Stonereach is boulders. Each belt is solid except for `passes_per_belt` angular gaps, and the gap centres are **staggered belt-to-belt** from `belts.seed`. Two properties follow, and both are held by tests over the live generator: no straight radial line is clear through every belt at once (`TestNoStraightRadialCorridorReachesTheRim`), so crossing the world means threading offset passes and detouring between them; and between any two belts the annulus is fully open while every belt has a pass, so the walkable space is one connected region (`TestGeneratedTerrainNeverSealsAWalkableRegion`, `TestEveryBiomeRegionIsReachableFromTheHub`). The detour is what turns the straight ~55 s line into a median ~105 s journey without adding empty distance — `TestOnFootJourneyToTheRimIsAtLeastFiveMinutes` measures a shortest-path search across the live walkable grid and guards the detour ratio rather than a fixed absolute time.

**Belts are permanent, indestructible terrain; scatter is destructible cover.** A barrier archetype has health `-1`, so a belt is never damaged, never fades, and its chunks evict and regenerate cleanly. Scatter archetypes carry health and are felled by projectiles like trees, leaving the same scar. `routes.clear_fill` thins the scatter inside a pass mouth so a chokepoint reads as an exposed, cleared lane; the lanes that connect one outpost to the next are laid along these passes in Phase 3.3, once outposts exist to connect. Every belt and scatter archetype declares its own `occludes_vision` and `visible_in_shadow`, so a boulder ridge is cover a body hides behind while a lava flow or an ice shelf is an impassable formation seen straight across — collision implies neither, exactly as [line of sight](#line-of-sight) requires. The client renders each archetype procedurally from the item's own id (`view.ts` `drawTerrainItem`), so two boulders never look alike and a belt reads as a biome-tinted solid mass.

### Outposts, safety, and travel

`server/internal/game/outpost.go` is the overlay that turns the world's radial safety into geography. The [world field](#the-world-field) still answers danger, biome, and grade by position — an outpost changes where a player is *safe*, never what the ground *is* — so the bit-identical Go/TS field and its coverage check are untouched. `web/src/game/outposts.ts` is the browser mirror; if the two disagreed, the HUD would tell a player they are safe where the server will still shoot them.

**Safety is now a bubble rather than a distance from the origin.** `World.Protected` is the field's answer *or* any outpost's `safe_radius`, and `World.Safe` is the hub *or* any bubble — the restricted fringe still protects without offering services. `World.serviceAt` is the third answer and the one the gates actually ask: `SetLoadout` requires `loadout` where the body stands, `Craft`/`CraftAmmunition`/`CraftRideable` require `crafting`, and the hub offers everything while a forward outpost offers only what its row declares. Nothing compares a radius of its own, which is what let this change be an overlay rather than a hunt through every call site.

**Discovery is proximity, and it persists immediately.** `stepDiscovery` unlocks an outpost the first time a body reaches its `discovery_radius`, awards the `discovery` XP source that was already priced in `progression.json`, and marks the character in `World.stateDirty`. The engine drains that set every tick through `drainDirtyStateLocked` and writes the state then and there rather than at the next autosave, because an unlock is a deliberate event like a loadout commit. A developer fixture discovers nothing: it has no character row to persist.

**A death goes to the nearest unlocked outpost, and nothing else moves a player.** `nearestUnlockedOutpost` is the one rule, shared by `Respawn` and by the stale-position `recallDestination`, so logging out can never reach somewhere a death would not have. There is no destination menu and **no fast travel of any kind** — no teleport between outposts, loaded or empty. A Deadlands death therefore lands on a Frontier outpost, which is why the rim needs no special rule. Respawn is still instant; the ~5 s timer stays with Phase 4.2.

**Exit invulnerability is granted on the crossing, not on the standing.** `updateExitInvuln` fires on the protected→unprotected edge only, so safety never refreshes it and a body already outside cannot re-arm it without going back in. It refuses incoming damage whole for `session.exit_invuln_seconds`, and `useAbility` clears it on the body's own hostile use — a projectile, a blast, or a dispel — so it can cover a transition out but never a free approach to a fight. It reaches every viewer as the existing `Entity.invulnerable`, because what an attacker plays around has to be visible.

**Terrain defers to an outpost's footprint.** `insideOutpost` joins `fixtureOverlaps` in the chunk generator, so neither scatter nor a ridge belt materialises inside a `safe_radius`. The ground an outpost stands on is open by construction rather than by hand-checked placement, which is what makes a respawn destination reliably standable.

### Rideable entities

`server/internal/game/rideable.go` is the traversal answer at this scale, and it is an entity rather than a movement state. A Gunslinger crafts a **vehicle** (a motorcycle); a Mage crafts a summoning crystal that materialises a **mount** (a warhorse). Both run one data-driven path from `rideables.json` — what differs is the class that builds one, its cost, its archetype, and its speed.

**Crafting is what spawns it.** `World.CraftRideable` mirrors `CraftAmmunition` — the same crafting-service gate, the same class-checked recipe, the same atomic spend — but its product is a world entity beside the player rather than anything in inventory. There is **one ride per owner**, so a second craft replaces the first, which is the rule `wall.go` already uses for the one wall per caster. A ride is transient: it is never persisted, and `RemovePlayer` reaps it, so it does not outlive the session that built it.

**Riding is server-authoritative movement and transport only.** `stepPlayer` hands a mounted body to `rideStep` before it can reach a stance, a dash, or a use, so a rider cannot fire, scope, guard, reload, or dash — the weapon is stowed, and the client drops those bits before they are even sent. `driveMount` moves the ride at `PlayerSpeed × ride_speed`, keeps the rider synced to it, and still applies the status layer: a slow scales a ride and a root or a stun stops it, so control effects are not shaken off by mounting. `Predictor.step` takes the same multiplier from the same table row, so a mounted step does not rubber-band.

**Damage routes to the ride, which is what ends a ride.** `World.damage` redirects a mounted body's incoming damage to `damageRideable` before armor or shields are considered — a ride has raw health rather than a mitigation stack — so the rider is never hurt while riding and `destroyRideable` puts it back on foot when the pool is spent. PvP protection and exit invulnerability both still gate the hit, because they are checked before the redirect. The consequence worth naming: a rider cannot die while mounted, only be dismounted, and every death path therefore runs on a body that is already on foot.

**Mounting is barred in combat.** `tryMount` refuses within `session.mount_lockout_seconds` of the body dealing or taking damage, tracked as `Player.LastCombat` in `damage` and in `useAbility`. That is what keeps a ride a way to shorten a journey rather than an escape from a fight. The interact button (E, or touch Use) both mounts a ride standing within reach and dismounts the one being ridden; it is the first thing to consume `ButtonInteract`, which had been plumbed through since Phase 1.5 without a consumer.

**On the wire** a ride is `Entity.Type MOUNT` with its own health, and its rider carries `Entity.mounted`, so both sides see the ride, its remaining health, and who is astride it. The renderer draws rides in a layer beneath actors so a rider always sits on top of its mount, and a mounted body is drawn holding nothing.

### Abilities and effects

Every deliberate action runs through one path. `data/tuning/abilities.json` owns the contract — cost, cadence, cooldown, windup, telegraph, dodge vector, delivery, and the status effects a hit applies — and weapons and spells hold only identity and point at it: a gun names the ability it fires, a staff names a spell that names the ability it casts. `game/ability.go` is the executor. `World.ability` resolves what the use button performs, `World.useAbility` gates it on the shared cadence and the ability's own cooldown, `World.spend` charges the declared cost, and `World.deliver` either launches immediately or creates a committed windup. Nothing branches on class or weapon shape: a magazine and a mana pool are two cost kinds on one path, and a spent magazine commits to its reload from inside `spend`. `World.ability` resolves the [selected loadout slot](#loadout-and-slots) rather than the weapon directly, so a gun, a gadget, and a spell all arrive as one shape; Phase 4.3's mobs call the same owner-agnostic telegraph emitter and delivery path. Each ability keeps its own lockout in `Player.Cooldowns` on top of the global `NextFire` cadence, which is the second resource axis the Mage kit is built on.

**Those lockouts are sent, not guessed.** A cooldown is authoritative state the client cannot derive: whether a use was charged at all depends on mana, ammunition, and the placement rules a cast is refused by, all of which the client sees a snapshot late. `SnapshotFor` therefore reports the *viewer's own* running lockouts as time left — `Entity` carries nothing, so nobody is sent an opponent's readout — and the client's action bar is a direct rendering of that set rather than a local timer started on the press. The client-side prediction it replaces was wrong in both directions: a spell cast the instant enough mana had regenerated showed no lockout at all, because the client's mana was still a snapshot behind and it suppressed the readout as an unaffordable cast, while a cast the server actually refused started a lockout that was never running. Time left rather than a deadline means no clock is shared with the browser, and an expired entry is simply omitted.

`game/effects.go` is the status layer: burn, slow, root, stun, knockback, shield, and blind. `Player.Effects` holds running statuses by effect ID rather than by value, so a balance edit retunes a status already on a body. `World.stepEffects` runs before the body acts and ticks burns in whole cadences, `World.damage` is the single path health is lost through — shields absorb first, and death clears everything the body carried — and `World.applyEffects` starts statuses from the projectile that carried them. Movement reads the layer rather than the reverse: a stun suppresses movement, dash, reload, and use; a root takes only movement; slows scale speed and take the strongest rather than compounding into an unanswerable root; a knockback overrides input and cancels an in-flight dash. PvP protection covers the status exactly as it covers the damage, because a slow landed from inside safety would be the offensive use of a safe zone the [invariants](game/design/invariants.md) forbid.

`data/tuning/effects.json` now carries every kind the layer runs. Phase 2.4 shipped the knockback a rocket's blast applies and the blindness a flashbang leaves — the one status that changes what a player is *sent* rather than what their body does — and Phase 2.5's element secondaries filled the rest: Fire's burn, Frost's chill, root, and freeze, Storm's concussion, Arcane's ward, and Earth's stagger, pin, and armor. **Armor is the one new kind**: mitigation with no pool behind it, taken as the strongest rather than compounded, applied in `World.damage` before a shield absorbs — so a ward spends its pool on what actually got through. Two wards can never stack into immunity, and validation refuses a multiplier of zero, which would be exactly that. Active effect IDs travel on player entities, so no content row needed a protocol change.

Windups live in `game/telegraph.go`. A use pays its cost and starts its cooldown immediately, locks authoritative origin and aim, and emits one AOI-culled `TELEGRAPH` entity. Pending ends at `windup_ms`; delivery happens once on that transition, active persists for the row's `active_ms`, and resolved supplies the flash/fade for `resolved_ms`. Death cancels a pending delivery directly into resolved. The fixed geometry is important counterplay: a caster may move or turn after committing, but cannot drag a warning over a player. Immediate abilities retain rewind; a windup starts at server receipt and is not backdated, so a client cannot erase the visible warning by claiming an old fire time. Circle, cone, line, and ring are validated shapes with one wire and renderer path. The starter Fire bolt exercises the line path; the Sentry row selects that same shape vocabulary while its timing, projectile, and AI remain Phase 4.3 content.

### Gunplay

`game/gunplay.go` owns where a shot actually goes and what a committed stance costs. Nothing in it reads a class: a weapon either declares a recoil pattern, a spread, a scope, or a guard, or it does not, so a staff runs the same path and simply has none of them.

**Recoil is a fixed pattern, never a random cone.** Each gun declares `recoil.pattern` — signed degrees, one entry per successive shot — and each entry is a *step* applied to a muzzle offset that persists between shots, so the weapon walks a shape rather than jumping between a few fixed angles. Every pattern's first entry is zero, so a settled first shot is always true. `recovery_ms` of quiet settles the offset linearly back to aim and returns the pattern to its first entry, which makes burst discipline the thing that controls a weapon; `Recoil.MaxDegrees` bounds the accumulation at half of one full walk of the pattern, because a magazine longer than the pattern repeats it. Because the pattern is data and deterministic, it is a shape a player learns and compensates for rather than a dice roll, and two characters with the same category fight with the same recoil.

The offset is a pure function of the last shot and the time since — `World.recoilDegrees` — rather than something integrated per tick, so a rewound resolve and a snapshot always agree on it. It travels on the wire as `Entity.recoil_degrees` alongside `Entity.shots`, the body's total shot count, because recoil is only a skill axis if it can be *seen*: the renderer rotates the drawn weapon by the offset, and every increment of the shot counter shoves the weapon back along its own axis with a muzzle flash. The camera itself is only knocked when the selected weapon fires an explosive — a launcher — so ordinary gunfire never shakes the view it has to be aimed through, and a thrown gadget's blast, which goes off away from the body, never does either. Gunplay belongs to the weapon slot alone, so a thrown gadget never walks the gun's pattern.

**Move-spread is the accuracy-against-mobility trade.** A weapon declares a standing floor and a moving maximum, and the simulation interpolates between them by how fast the body is actually travelling. The magnitude is drawn from `splitmix` seeded by the shooter's ID and its own total shot count, so a shot is exactly reproducible in a test while remaining unpredictable in a fight, and no shared RNG state couples two players.

**Weight class is the balance axis and never touches damage.** `combat.weight_classes` scales movement, recoil, and move-spread; `World.handlingScale` multiplies the weight class, an active scope, and a raised shield, and composes with `World.movementScale`, which is what statuses do — a slowed body carrying an LMG pays both. The client applies the identical multiplier through `Predictor.step`, so a scoped or shielded step does not rubber-band.

Delivery gained three shapes, all validated at load. A **multi-pellet** projectile fans deterministically over its declared cone and divides the band's damage between its pellets, so a full connect is worth exactly one band hit and the shotgun's identity is the condition it imposes rather than extra damage. **Falloff and hard maximum range** live on the projectile: a round tracks how far it has flown, decays linearly from `falloff_start` toward `falloff_min`, and stops at `max_range` regardless of lifetime left. A **blast** resolves at the impact point, reaching everyone inside its radius with the band and the effects it declares.

**Hitscan is the sniper's exception and it is gated.** An ability may only land instantly if it declares `requires_scope`, and it must claim the `scoped_commit` dodge vector, which names what replaces travel time: the peripheral blackout and movement penalty of scoping. `World.hitscan` resolves along the line with the same rewind a fired projectile gets, cover stops it before anything behind it, and if nothing is reached inside the cap the round continues as an ordinary travelling projectile that starts already carrying the distance it covered instantly. Scoping is held rather than toggled, widens the snapshot the server sends by `scope.view_bonus`, and reaches the wire as `Entity.scoped`, because the commitment is what an opponent plays around.

**The riot shield** is a gadget whose ability declares a `guard` instead of a delivery: while its slot is selected, holding the use button raises it. It blocks bullets and projectiles arriving inside its frontal arc, measured against where the body is aiming; it slows its user; and it locks fire, since the button that holds it up cannot also shoot through it. It does not stop a blast that goes off around it, which keeps it an answer to a burst opener rather than an answer to everything.

**Durability is the shield's health, and it is what stops the shield being invulnerability with an arc drawn on it.** `guard.durability` is a pool the barrier spends on what it stops: `World.guardAbsorb` charges every blocked round to it and passes the overflow through to the body, so a nearly-spent shield cannot eat a rocket whole. At zero it breaks — `blockedBy` stops blocking, `stepPlayer` refuses to raise it, and the client drops the predicted movement penalty with it, since the authoritative remainder rides the wire. `stepGuard` repairs it at `regen_per_second` after `regen_delay_ms` of quiet, only while it is lowered, and returns it to service only once it is whole, so it cannot be flickered back up on one point of durability. Recovery is resolved from the shield the body carries rather than the slot it is looking at, so a spent shield keeps repairing while its owner fights on with the gun in the next slot; a respawn returns it intact. `Entity.shield` and `Entity.max_shield` carry the pool to every viewer, because when a shield is about to break is exactly what both sides are playing around. Validation refuses a guard with no durability or no recovery.

**Smoke and flashbangs are deployables and control, never damage.** An ability may declare a `deployable` — an entities.json archetype, a radius, a lifetime, and a reveal radius — and `game/deployable.go` materialises it where the thrown canister stopped. Only a field that declares `conceals` is checked by the smoke rule at all, which is what separates smoke from the [Mage's burning and freezing ground](#spells-and-the-mage-kit): a patch of fire is plainly visible, and hiding what stands in it would be a rule no player could read. A field is not a collider: it changes what can be seen, never where a body may walk, and validation refuses an archetype with collision geometry. A concealing cloud **casts a shadow like terrain** rather than swallowing only what it covers (see [line of sight](#line-of-sight)): a body behind it is hidden, a body half out of its rim is still seen by the any-part rule, and a body standing inside it sees only a small circle bounded by the field's reveal radius — enough to fight what it is touching and to peek out at the edge. The renderer answers for the drawn half of that agreement with overlapping puffs laid out to cover the authoritative radius and a soft, ragged rim that may spill slightly past it. A flashbang needs no field at all: it is an ordinary blast that applies a `blind` status, and `World.blinded` is checked once at the top of `SnapshotFor`.

A blast resolves wherever its round stopped — an impact, the rim, the end of its range, or simply the end of its throw — because a canister reaches the ground far more often than it hits a body, and one that only went off on a direct hit would effectively never go off at all. `resolveBlast` marks the round detonated, so a shot that is stopped and then reaped cannot resolve its area twice.

Both are enforced on the wire rather than drawn over on the client. A blinded viewer is sent nothing but its own body; a viewer looking at a cloud is not sent what it covers. **A cloud never hides what the viewer owns**: concealing a body's own rounds made smoke read as a wall it could not shoot through — the round vanishing at the edge and reappearing past it is a vision rule pretending to be collision — so players, projectiles, and telegraphs the viewer owns skip the concealment test. Everything else is tested against its own extent: a player and a projectile by their collision radius, a telegraph by its anchor point, since the shape it warns about reaches far outside its own origin. What smoke is bought for is hiding the opponent and the shots *they* fire. Terrain is the deliberate exception in both cases: static cover blinking in and out as a cloud drifts would desynchronise the client's own collision prediction, and the cloud drawn over it already hides it. The renderer draws a field as drifting puffs seeded from its own ID — nothing describing how a cloud looks travels on the wire — and paints a blindness as a white sheet over the stage for as long as the status runs. **A puff is a tinted sprite off one shared disc texture, not its own filled circle**: every field on screen then batches into a single draw, and a drifting puff re-uploads four vertices per frame instead of a tessellated ring, which is what keeps a field a player can place repeatedly from costing geometry proportional to how often they place it.

### Line of sight

`game/visibility.go` makes selected terrain authoritative for sight as well as movement and projectiles. Every entity definition carries two independent attributes: `occludes_vision` decides whether its collision objects block sight, while `visible_in_shadow` decides whether its silhouette remains readable in a visually occluded area. Collision alone does not imply either property, so trees and future decorations remain visible landmarks without becoming cover, while fixed walls and Mage wall segments enable both. A destroyed or expired item stops occluding immediately; graceful deletion is visual feedback, not invisible cover. Terrain itself is always retained inside the AOI so the player sees the obstruction and client prediction keeps the same colliders.

**Occlusion is a property of the whole silhouette, not the target's centre.** `sightOccluders.clearLine` reports a body visible when *any part* of it — a circle of its collision radius — has an unobstructed line, so a player peeking a shoulder past a wall corner is seen rather than hidden by a centre-only ray. The direct centre line is tested first, so an unobstructed target still costs a single ray; only when the centre is blocked are the two tangent edges and the near cap toward the viewer sampled. The sight-blockers — vision-blocking terrain plus smoke clouds — are collected once per viewer-send by `World.collectOccluders`, from the same spatial index collision and snapshot interest use and bounded to the viewer's own interest square, rather than rescanning the world per candidate entity. Visibility is therefore one answer rather than a second one that can drift from collision.

**Smoke is now a sight-blocker in its own right rather than a containment special case.** A concealing cloud casts a shadow exactly as a wall does: a body behind it is omitted. Two rules distinguish it from terrain. First, it never hides what the viewer *owns* — a cloud that shadowed a body's own rounds would read as a wall it could not shoot through — so the viewer's own players, projectiles, and telegraphs are tested against terrain alone (`sightOccluders.visibleTerrain`). Second, a body standing *inside* a cloud sees only a small circle around itself: the cloud it stands in is skipped as an occluder and its `reveal_radius` gates what reaches the wire, so everything past that circle is swallowed while a body at the rim can peek just outside the smoke. **Area fields are exempt from occlusion entirely** — a firestorm, a blizzard, or a cinder patch behind a wall is ground the player is entitled to play around, and a smoke cloud is exactly what explains a body vanishing behind it, so `SnapshotFor` sends every deployable within the AOI regardless of cover.

`SnapshotFor` otherwise omits dynamic players, projectiles, and telegraphs behind that geometry. Automatic target acquisition reuses the same `sightOccluders.visible` predicate; homing rounds and chain jumps therefore skip a nearer hidden body and may choose the next visible one. Manual aim and ground placement do not acquire a body and deliberately bypass it. This preserves the class matchup: a wall stops sight, movement, and direct rounds but never blocks a ground-placed area effect, while smoke disrupts body-dependent acquisition without becoming physical cover. Acquisition draws its candidates from the same index, so it costs what is in range rather than what is in the world.

The client packs the nearest shadow-casting circle/box colliders — terrain plus every smoke cloud the local player is not standing inside — into a bounded GPU uniform block. A half-resolution fragment shader analytically tests the local-player-to-pixel segment against those shapes and emits a 27%-opacity veil only for blocked pixels; this replaces per-frame CPU ray fans, visibility-polygon rebuilding, and triangulation. **The shader declares `precision highp float;` and every test stays at coordinate scale**, both of which the circle path needs: Pixi prepends `precision mediump float;` to a fragment that declares none, and on a driver that honours mediump as fp16 the old quadratic's `b² − 4ac` — the square of a screen coordinate, squared again — saturated to infinity and left a NaN that compares false everywhere, so circular occluders (smoke and Mage stone walls) silently cast no shadow while axis-aligned boxes, which only subtract and divide, kept working. The disc test is now the distance from the segment to the centre along a normalised direction, which is both cheaper and in range whatever precision it is compiled at. When the player is inside a cloud, the shader also veils every pixel past its reveal radius, which is what draws the small visible circle and the rim peek. Entities whose shared definition sets `visible_in_shadow`, and all area/smoke fields, render in layers above the veil. This is presentation only—the server's omission remains the security and targeting rule. Snapshot omission no longer drops a hidden actor outright: the client fades it out over a short window (and fades a reappearing one back in), freezing it at its last-seen position rather than blinking it in and out, and collects it only once it has faded away. **That fade is the only collection path, so nothing may be pinned in sight.** Fields were, on the reasoning that they are exempt from line of sight — but that exemption is a server rule, enforced by `SnapshotFor` sending a field through cover, so a standing field is in the present set anyway and pinning it client-side only meant an expired one never faded and was therefore never collected. Interpolating past the last sample deliberately holds the final one rather than reporting absence, so it can never report it either: every cloud a player had ever seen stayed in the scene graph, invisible at the zero opacity its last delete frame left, still animated and re-batched every frame. A sample buffer that has gone quiet for far longer than the fade window is now collected outright as the backstop, so an actor can never outlive the snapshots feeding it whatever its fade is doing.

A blinded owner cannot acquire an automatic target at all. Ordinary firing still accepts an aim direction, so blind fire remains possible without letting homing or chain delivery use target information the owner is not receiving.

**Special ammunition** is the exception to the magazine. A launcher's ability spends a carried material (`cost.kind: "material"`) rather than rounds, so it has neither a magazine nor a reload: when the stack is empty, it stops firing until more is built. `data/tuning/ammunition.json` holds the recipes, `World.CraftAmmunition` runs them through the same safe-zone gate an ordinary craft uses, and what they produce lands in the carried inventory — which means Phase 4.2's death drops will treat rockets as the carried, losable resource they are.

### Spells and the Mage kit

`data/tuning/spells.json` is the settled 5 × 4 grid — Fire, Frost, Storm, Arcane, and Earth, each authored to tier 4 so [affinity's 4 + 2 build](game/design/mage.md#element-affinity) is satisfiable. A spell is identity over one ability, exactly as a gun is: element, tier, unlock level, and the ability it casts. Everything that costs, gates, warns, or delivers lives on the ability row, so nothing in the simulation branches on a spell ID and a Sentry that declares the same fields gets the same behaviour.

**Spell tier is a commitment axis, not the vertical one.** Every spell spends mana; every spell above tier 1 also holds its own cooldown, which is the second resource axis the class is built on and the reason spending everything and missing exposes a Mage the way an empty magazine exposes a Gunslinger. `TestShippedSpellsPriceThemselves` holds the ladder: within an element, each tier costs more mana and locks out longer than the one below it. A spell's tier selects a delivery window and shared damage band; item power comes from the staff assembly, not from a spell-specific damage number.

Six delivery shapes were added to the one ability path, each a field on the contract rather than a branch in the executor. `World.deliver` resolves an immediate cast and `World.deliverAt` a completed windup; both call the same `World.resolve`, so a spell is delivered by the code a rocket is.

- **Placement** (`placement.range`) moves where a cast lands: a point that far along the aim the cast committed to. The wire carries an aim direction and no cursor distance, so the range belongs to the ability rather than to the player's pointer — and the telegraph is anchored at exactly that point, which is what a `ground_indicator` dodge vector now formally requires.
- **Fields** are the deployable a smoke canister already used, extended to pulse. A field may declare a band, a fraction of it, a cadence, and the statuses each pulse applies; `World.stepField` catches up whole cadences so a patch deals the same total however the tick rate divides it. `final_effects` land once, on the pulse that closes the field, which is how Blizzard's stacking slow ends in a stun. A `trigger` field is a trap: it lies inert, is sprung by the first hostile body to reach it, and is spent. PvP protection covers a pulse exactly as it covers a bullet.
- **Blink** (`blink.distance`) is a walk rather than a jump. `World.blink` steps toward the destination and stops at the last free ground, so a blink can never cross cover — a mobility tool that ignored walls would make every piece of cover in the world optional. `blink_on_hit` is the conditional version: Skyfall carries its caster to the impact only when the area actually reached someone.
- **Chain** arcs a landed hit onward from the body it struck rather than from the caster, never onto the caster and never twice onto one body.
- **Homing** is bounded steering on the projectile: a turn rate in degrees per second and an acquisition range. The cap is the counterplay — validation refuses anything past a full turn per second, because a round that turns freely is a lock-on and travel time stops being a dodge vector.
- **Cleanse** is the dispel, and it is deliberately indiscriminate: it strips every status from the caster and from hostile bodies inside its radius, buffs and shields included, and returns mana per status removed. Casting it while your own ward is up throws the ward away with the enemy's, which is what makes it a read rather than a free button. Stripping is an offensive act, so PvP protection bounds it.

**The stone wall is the first player-authored terrain.** `game/wall.go` materialises `wall.segments` ordinary world items, laid perpendicular to the cast's direction, so collision, projectile damage, destruction, snapshots, and line of sight all reach it through the code that already carries trees — the segments use circle geometry rather than boxes because the world's boxes are axis-aligned and a wall stands at whatever angle it was cast. It is destructible, it expires, and there is **one per caster**: raising a second drops the first through the shared graceful removal. Two placement rules are checked *before* the cast is charged, so a refusal costs nothing: a wall may not be raised inside a safe zone, because safety is never an offensive tool, and it may not be raised on top of an actor, because a wall that boxed a player in would make it one.

**A wall's lifetime is part of the rewind history.** Every entity carries the moment it was raised and the moment it was destroyed or expired, and rewound projectile resolution tests `Entity.presentAt` rather than the current world: a shot rewound to before a wall went up passes through where it now stands, and a shot rewound to while it stood is stopped by it even though it has since come down. Terrain destruction is recorded through `Entity.Delete` for the same reason, which also closes the pre-2.5 gap where a tree felled inside the rewind window disagreed with the cover the shooter saw.

**Mana crystals gained the two axes a rarer crystal has to earn beyond a larger damage figure.** `area_radius` widens a blast, the field it leaves, and the telegraph that warns about both *together* — an area that outgrew its own warning would be damage the target was never shown — and `element_damage` biases one school. The bias is the one modifier that depends on what is being cast rather than on what is holding it, so `crafting.Bias` applies it against the selected slot's element after `crafting.Apply` has done everything the staff does to every spell alike. Both are bounded by the same `[0.5, 2]` band every other modifier is, and only a mana crystal may claim either.

### Loadout and slots

`server/internal/loadout` owns what a legal equipped set is; it holds no world state, so the simulation, the store, and the client agree on one answer without any of them owning the rule. A set is content IDs by slot — one weapon, five gadget slots, and six spell slots from `data/tuning/loadout.json` — and never a copy of what those rows hold, so a balance edit retunes an equipped kit in place.

Both classes lay out over **one six-slot action bar**, which the tables enforce (`weapon_slots + gadget_slots == spell_slots`). A Gunslinger's slot one is its weapon and slots two through six are gadgets; a Mage's six slots are spells, cast through whichever staff it holds. `loadout.Equippable` intersects the live rows of the character's class with its unlock ledger, and `loadout.Validate` refuses unowned content on the mutation path, so hiding an option in the menu is never what stops it being equipped. `loadout.Bar` is the single answer to "what does the use button do", and `Input.selected_slot` travels on every input so the server resolves the button against the slot the player actually had selected. Keys 1–6 and the mouse wheel select it; touch gets six buttons. An empty slot performs nothing, which is a slot the player has not filled rather than an error; a Gunslinger's gadget slots hold the riot shield, the smoke canister, and the flashbang. A Mage's slot one falls back to its staff's own declared spell, so a set emptied by a content withdrawal can still fight.

The Mage's affinity rule is the specialisation cost: a tier-N spell needs N−1 other spells of its element in the same set. The rule's shape is locked by [`mage.md`](game/design/mage.md#element-affinity) and only the multiplier is tunable, so the table validates that a tier-4 signature stays equippable inside the bar it shares.

**The safe-zone lock is the economy's keystone.** `World.SetLoadout` refuses any change from anywhere that does not offer the `loadout` service — the hub, or an outpost whose row declares it ([outposts](#outposts-safety-and-travel)) — and from a dead or lingering body, so what a player owns is committed before the fight rather than answered into it. It is enforced on the mutation, not on the UI: a client that ignores its own disabled controls is still refused. Respec inside safety is free — nothing is charged or consumed — and a committed change arrives as a fresh kit, full magazine and no cooldowns, both of which are only reachable where the lock already allows the change.

Commits travel over the same socket as everything else authoritative: `CLIENT_LOADOUT` carries the requested set, and the server always answers with `SERVER_LOADOUT` carrying the set that actually holds, plus a rejection reason when it refused. That reply is not terminal the way `SERVER_ERROR` is, so a refusal never drops the connection, and nothing is shown as equipped until it arrives. A committed set is persisted immediately rather than at the next autosave, because it is a deliberate commit rather than incidental world state. Loadouts cannot be edited outside the world at all: there is no HTTP mutation path, so a character logged out in the Deadlands cannot respec from the home screen.

The weapon slot holds something usable, and it may name either a `weapons.json` row — the stock configuration the starter kit and every level grant hand out — or a [crafted instance](#recipe-blueprint-crafting) of one. `crafting.Inventory.Equipped` resolves both to the same shape, so nothing downstream branches on which it was, and both are gated the same way: the instance must be one this character owns *and* its category must be in the ledger. Materials and components never reach the action bar; they live in the inventory surface.

`loadout.Resolve` runs on every join. Retired IDs follow the retirement chain, a withdrawn weapon is replaced by the class starter rather than leaving a character unarmed, and an arrangement a content change made illegal is repaired highest-slot-first so the casualty is the last thing equipped rather than the character's signature. An empty record resolves to the class default — the first weapon the character owns, plus the owned content of its slot kind packed from slot zero. Anything the [unlock ledger](#progression-and-unlocks) does not own is unequipped by the same pass, so a set can never outlive the ownership behind it. When the manifest `version` moved or the set had to change, the resolve reports the **global respec** the balance patch entitled the character to; since respec is already free, that grant is the re-validation plus the notice, and it clears the moment the player next commits.

### Progression and unlocks

`server/internal/progression` owns the permanent character axis: a **flat unlock ledger**, the XP curve, and the starter kit. Like `loadout`, it holds no world state, so character creation, the simulation, and the store reach one answer. A character record stores level, banked XP, and a sorted array of owned content IDs — never a derived stat and never a per-tree structure, matching the [flat-ledger contract](game/design/gunslinger.md#progression). Entries are only ever added: retiring content maps an entry onto its replacement, and a refunded retirement is owed against the material ledger rather than confiscating the slot.

Which content a level grants is declared on the content row itself. Every weapon, spell, and gadget carries an `unlock_level`, so adding a row means editing one table rather than keeping a second mapping in step, and `Tables.UnlocksThrough(level)` is a scan of those rows. Because the ledger stores bare IDs, validation requires unlock IDs to be unique across the three content tables and every row to unlock at a level inside the cap — content no level grants would be unreachable for anyone whose starter draw missed it.

**The starter kit** is rolled at character creation, not at first join, so a zero-material character owns a coherent loadout before it enters the world: one weapon drawn at random from its class's basic set, plus a random draw of the basic content of its slot kind, sized to fill the action bar. The draw is seeded from the character's own ID, so it is stable across re-derivation while two characters get different opening tools. Nothing drawn is exclusive — every row in the basic set also carries an unlock level, so a bad draw is a starting flavour, not a permanent gap.

`progression.Sync` runs on every join, before the loadout resolves, because what a character owns decides what it may equip. It follows retirements, tops the ledger up with everything the character's level has come to grant — which is how content added at a level a veteran is already past still reaches them — and rolls a kit for a record that predates the ledger entirely.

XP is awarded through named sources (`progression.sources`), a vocabulary the loader requires to be priced in full and refuses to extend from data alone. `player_kill` and `discovery` have triggers today: `World.creditKill` awards it to whoever the [combat log credits](#damage-attribution-and-combat-log), so progression follows damage dealt rather than the last hit, and developer fixtures award nothing on either side. `discovery` is awarded by [outpost discovery](#outposts-safety-and-travel); mob kills and harvesting are wired by the phases that build them. `progression.Advance` carries surplus XP into the next level and stops banking at the cap.

Because a player kill is the only trigger until Phase 4.3, content above the opening kit would otherwise be unreachable — and therefore unexercisable — on a fresh server. `POST /api/admin/progress` is the developer-mode answer, bounded by `progression.admin_grant` and running through the same grant path: it sets the level and adds everything the levels now held unlock. Lowering a level never confiscates an unlock, because the ledger is permanent by design.

A grant marks the body, and the tick loop drains those marks: each is persisted through `SaveCharacterProgress` — a separate statement from the world-state save, so a save written by a body that entered before a grant cannot roll it back — and pushed to its owner as `SERVER_PROGRESS`. The permanent axis also rides the welcome. Nothing about it is polled, so a level-up widens the Loadout section's options immediately.

### Recipe-blueprint crafting

`server/internal/crafting` owns recipe resolution, cost aggregation, compatibility, and the derived changes a finished item makes. Like `loadout` it holds no world state — the safe-zone gate needs a position, so `World.Craft` owns that — and it stores nothing derived: a crafted item is a weapon-row reference plus the component filling each blueprint slot, and every implied value is recomputed from the tuning tables at use time.

`components.json` separates three concepts: generic blueprint geometry, independently costed component recipes, and finished-weapon recipes keyed by the resulting `weapons.json` row. A gun blueprint exposes receiver, barrel, action, feed, and sight. `crafting.Result` compares a complete submitted arrangement against every live recipe and requires exactly one match; `crafting.Validate` then checks class, unlock ownership, slot fit, and the selected recipe. The category carried in the wire request is only a stale-preview check. It cannot turn pistol parts into a sniper. Load-time pairwise overlap validation prevents two recipes from accepting the same complete arrangement.

A staff blueprint exposes only crystal and stave. Mana crystals are independently priced component recipes with bounded all-spell modifiers. Staves have no combat modifiers: wood and infusions establish a containment tier, and validation requires `stave.tier >= crystal.tier`. The workbench presents the two subassemblies separately, but `World.Craft` aggregates and spends both recipes atomically with the finished staff; no loose-part persistence or protocol shape is needed. Existing crafted items remain reference-only. `retired.json` maps every withdrawn pre-revamp component ID, and `World.craftedItems` rewrites both its ID and live slot name on join so old saves remain usable.

Modifiers merge multiplicatively across slots. Gun-part attributes cover magazine size, reload, projectile behavior, recoil, spread, and scoped movement. A scoped movement modifier remains clamped below one. Mana crystals may also set `spell_damage` and `spell_healing`; `crafting.Apply` carries those as derived `Ability` multipliers, projectile and hitscan damage consume `DamageScale`, and future healing delivery consumes the parallel `HealingScale`. A healing multiplier never grants a healing payload to an ability that has none. `interval_ms` remains forbidden, all multipliers are bounded to `[0.5, 2]`, and only a mana crystal may claim the two all-spell output attributes. Staves are the deliberate exception to the “every component modifies behavior” check because containment tier is their entire function.

**Crafting is gated to safe zones**, the other half of the [loadout lock](#loadout-and-slots): raw materials have to be hauled back before they become anything. `World.Craft` refuses from anywhere that does not offer the `crafting` service — the hub, or an outpost whose row declares it — and from a dead or lingering body, then validates the recipe and the carried materials before it spends anything. A refusal is atomic — no item, no spend — and reports what is missing per material, because "you are short three tempered plate" is actionable and "you cannot afford this" is not. `CLIENT_CRAFT` carries the request and the server always answers `SERVER_CRAFT` with the authoritative owned items and carried materials, plus a rejection reason when it refused; like `SERVER_LOADOUT` that reply is not terminal, so a refusal never drops the connection and nothing is shown as spent before the server confirmed it. A successful craft is persisted immediately: the item insert runs before the state that paid for it, and a failed insert abandons that state too, so a craft never takes the materials without leaving the item behind.

A weapon row may also carry a material `cost` of its own, and the heavy categories carry `requires_craft`: they have no stock configuration at all and can only be carried as an instance that was actually built. This cost is added to the required component recipes. `crafting.Inventory.Equipped` refuses a withheld row as a stock reference, so the gate holds on the mutation path rather than in the menu.

`progression.crafted_item_capacity` bounds how many crafted weapons a character may own and is also the capacity outcome the crafting surface owes. Developer fixtures cannot craft at all — an admin-spawned body has no character row to hang an item off.

Nothing produces a material yet — harvesting is Phase 4.1 — so the only source today is a developer-mode grant behind the [administrator wrapper](#administrator-authorization), bounded by `materials.admin_grant` and validated against the live material rows.

### Damage attribution and combat log

`World.damage` records the health actually removed after shield absorption and caps it at remaining health, so neither shield pressure nor overkill inflates contribution. `combatLog` maintains a per-target ledger for the current life. A lethal hit freezes that complete ledger into a `CombatKill` event and chooses the largest contributor; an exact tie goes to the contributor whose first effective hit was recorded earlier. Last hit remains on the event as `SourceID`, but does not decide `CreditID`.

`World.Contributions` exposes a live ordered view for boss progress, `World.LastKill` exposes the immutable current-death decision for drop ownership, and `World.CombatEventsAfter(sequence)` is a bounded cursor stream for independent consumers. Respawn clears the mutable ledger and last-kill lookup, while immutable damage/kill events remain until the 1,024-event ring rolls over. Consumers must advance a cursor; the log is an integration surface, not durable storage.

Balance values live in the [tuning tables](#tuning-tables), never in the simulation source. `game.Tuning` is a derived runtime view of them: `game.FromTables` fills it, and only the process-level rates (tick, send, AOI, rewind) are overridden afterwards from configuration. Persistent records store references and progression fields, not computed combat values, matching the [progression persistence contract](game/design/progression-and-crafting.md#persistence-and-versioning).

## Tuning tables

`data/tuning/*.json` holds every balance number as versioned data. The Go server embeds the directory through `data/data.go` and parses it in `server/internal/tuning`; the Vite client imports the same files from `web/src/tuning.ts`. One directory, two consumers, no duplicated literal — a balance edit moves the authoritative simulation, client prediction, and the renderer together. `data/tuning/README.md` documents the file-by-file schema and the editing rules.

The tables cover simulation rates, session windows, common entity defaults and their admin metadata, world geometry, chunk size, danger bands, the world field's seed/lattice/reward curve/coverage rule, procedural terrain density, fixed fixtures, the player body and universal dash, weight classes, damage bands, combat-role metadata, elements, abilities, status effects, weapons, spells, blueprint/component layouts and the components that fill them, material grades, kinds, and rows, special-ammunition recipes, crafted-to-spawn rideables, mobs, biomes with their summaries and ambient palettes, outposts with their no-PvP/discovery radii and service sets, and retired IDs. `spells.json` holds the complete 5 × 4 grid and `effects.json` a row of every status kind the layer runs. Rows exist only where a design document has settled the content.

Damage is the clearest expression of the contract. No weapon, spell, or ability carries a damage number; a damaging ability points at a sustained, burst, or heavy-burst row, and weapons and spells carry no combat numbers at all. `Tables.ResolveDamage` combines the band anchor and cadence with the derived item multiplier to report per-hit damage, DPS, and raw TTK. Editing one band row retunes every dependent item, while persisted characters continue to store only IDs and need no migration.

Loading validates rather than trusts. In addition to structural and cross-table checks, every direct damaging ability must use its band's cadence, both classes must cover all seven combat roles, rarity grades must be contiguous and bounded, component tier must be funded by a material of that tier, and every non-Common component must include a horizontal modifier. The loader enumerates complete recipes and enforces the assembled damage, effective-health, combined-item, and minimum-TTK limits before accepting the tables. The existing geometry, delivery, gunplay, spell-shape, crafting-coherence, modifier, and telegraph checks continue to report all problems in one pass. `manifest.json` carries the content and schema versions; a schema mismatch fails with a request for the forward migration.

Because the tables are bundled into the client at build time, the server and the client must ship from the same build — which `make build` and the Dockerfile both do. Delivering tables to an already-running client, so that simulation constants can move without redeploying, is the separate versioned welcome/tuning message tracked in [`TODO.md`](../TODO.md) Phase 8.

## Network model

`proto/game.proto` is the protocol contract. The game socket accepts and emits only binary Protobuf frames; REST uses JSON because those low-frequency account surfaces benefit from ordinary HTTP semantics. The repository includes small schema-specific codecs in Go and TypeScript instead of requiring `protoc` at runtime. Golden-wire tests detect schema-number or wire-type drift.

Entity types reserve distinct paths for player, projectile, mob, drop, node, telegraph, deployable, boss, world item, and mount. Common optional fields carry mass, health, element, squad ID, viewer-relative allegiance, invulnerability, logout linger, mounted state, active effects, and telegraph phase/geometry. Each world-item collision component carries its owning entity, shape, and circle or box dimensions. Protobuf omits zero/default fields, keeping unused future state cheap. Input bit 128 is `INTERACT`; desktop binds E and touch exposes Use, and since Phase 3.3 it mounts and dismounts a ride. Allegiance and telegraph phase are compact enums so adding content does not introduce ad-hoc strings into the 20 Hz path.

Defaults:

| Concern | Value | Behavior |
|---|---:|---|
| Simulation rate | 60 Hz | Fixed authoritative physics and client prediction |
| Snapshot send rate | 20 Hz | One AOI-filtered snapshot per client every third tick |
| Client input rate | 60 Hz | Sequence-numbered current input state |
| Interpolation delay | 100 ms | Remote entities render between buffered snapshots |
| Rewind window | 200 ms | Client fire time is clamped to this server window |
| AOI half-extent | 1,200 units | The full 2,400 × 2,400 camera square is retained; entities beyond either axis are culled |

### Snapshot bandwidth budget

`protocol.TestSnapshotBandwidthBudget` measures deterministic fixtures through the production encoder. For 20 players, 40 projectiles, and 30 world-item entities plus their collision components, the pre-expansion fields encode to 8,354 bytes and the expanded entity state to 9,720 bytes: +1,366 bytes, or 16.3%. Ten live line telegraphs bring that representative combat snapshot to 10,900 bytes (218,000 bytes/s at 20 Hz, before WebSocket/TCP/TLS overhead). The equipped loadout is deliberately not in that figure, and neither are owned crafted items or carried materials: all three travel on the welcome and on their own replies, never on a snapshot, because each changes only on a deliberate action inside a safe zone and paying for them twenty times a second would be pure waste. The viewer's own ability lockouts are the one per-viewer field that does ride every snapshot, because they change on a use rather than inside safety; they are bounded by the six-slot action bar and cost a few dozen bytes only while something is actually cooling.

The development dense fixture — 100 players, 200 projectiles, 25 telegraphs, and 80 world items with collision components — is 43,785 bytes, or 875,700 bytes/s at 20 Hz before transport overhead. The enforced guardrail is 64 KiB per AOI snapshot (1.25 MiB/s at 20 Hz).

`game.TestConcurrentLoadHoldsTheSnapshotBudget` measures the same guardrail through the live world at the new scale and terrain density rather than through a hand-built fixture: 50 and then 100 bodies packed into one battle-density cluster, firing, stepped for half a second, and every one of their snapshots encoded. The largest snapshot is 11.0 KB at 50 bodies and 15.0 KB at 100 (300 KB/s per client at 20 Hz) — a sixth of the ceiling, with roughly 22 world items inside an interest square. `TestSpreadPopulationKeepsResidencyBounded` covers the other axis: 100 bodies spread across the Frontier hold about 1,300 chunks of a world with roughly 4,400, at about 1.1 ms per tick. This is still a ceiling rather than a production target: Phase 8 owes delta/compressed snapshots and priority tiers before the 100+ player claim is production-ready. Any schema or density change that crosses the guardrail fails the protocol suite and must bring a bandwidth strategy with it.

The rectangular camera AOI covers `4 / π` (about 27%) more area than the former inscribed circular cutoff at uniform density. That cost is deliberate so visible corners never lose state, but the byte guardrail and Phase 8 spatial-index/load-test work remain necessary before raising the configured half-extent.

### Prediction and reconciliation

The client applies local movement and dash immediately at the same fixed rate as the server and retains each input sequence plus its predicted motion. Because the dash is spread over a fixed tick count instead of a single displacement, the client counts down the same dash ticks at 60 Hz and records one motion per input, so replay reproduces the server path exactly. Every authoritative local-player snapshot carries `acknowledged_input`. The client resets to that position, removes acknowledged motions, and replays the remainder through the same circle/AABB collision rules. The current snapshot's AOI collision components replace the previous set before reconciliation, so destroyed or out-of-range world items stop blocking predicted movement.

**Statuses are predicted, because a status that only the server knows about is a stutter.** Active effect IDs already ride the local body's own snapshot, so `tuning.movementStatus` resolves them into the same four answers `game/effects.go` gives — the strongest slow, a root or stun that immobilises, and a knockback — and `Predictor.step` applies them beside the handling multiplier. A slowed body therefore walks at the slowed speed locally instead of running ahead and being snapped back by every reconciliation; a rooted or stunned body predicts no step and refuses to spend a dash the server would have refused, and a stun drops both committed stances. A knockback is the one case prediction deliberately does nothing: its direction never reaches the wire, so the client predicts zero motion, cancels its in-flight dash exactly as the server does, and lets reconciliation supply the whole displacement rather than fighting it with input. The residual error is bounded by the snapshot interval — a status that expires between snapshots is briefly still predicted — and reconciliation closes it in one frame.

The server never accepts client position, health, damage, cooldown, ammo, or mana values. It accepts buttons, aim direction, input sequence, and client fire time only. Invalid/stale sequences are dropped.

### Entity interpolation

Remote entities retain up to eight received samples. Rendering occurs 100 ms behind receipt time and linearly interpolates position and aim between the two surrounding samples. Telegraph progress interpolates only inside one phase; phase changes remain discrete so an active/resolved state is never pulled earlier by interpolation. Graceful-removal opacity follows authoritative deletion progress. The local actor uses its predicted position instead. Entities absent long enough to leave the AOI are destroyed client-side.

### Lag compensation and server rewind

The world retains a short timestamped position history for every player. When firing, the server:

1. clamps the claimed client time to `[server_now - max_rewind, server_now]`;
2. samples the shooter at that historical time;
3. creates the projectile at that historical muzzle position;
4. fast-forwards it in fixed substeps, testing against interpolated historical target positions and current static cover;
5. inserts a surviving projectile into the live simulation.

This preserves the [projectile dodge requirement](game/design/invariants.md) while compensating for reasonable network delay. It does not turn ordinary weapons into hitscan. Equal-timestamp history samples prefer the latest state, which is important for respawns and authoritative repositioning.

### Backpressure and connection lifecycle

Each client has a two-message outbound queue. If a slow client fills it, an older snapshot is discarded for the newer authoritative state rather than blocking the world tick. WebSocket reads are limited to 2 KiB, joins have a deadline, ping/pong detects dead peers, writes have deadlines, and reconnect attempts are exponentially backed off and capped at five. During reconnect the rendered world remains visible under an input-blocking overlay.

## Rendering and interface

Pixi.js is the committed 2D backend. Its WebGL batching of `Graphics` primitives covers the 100-player density target, and the design's [procedural visual language](game/design/visual-direction.md) needs no texture pipeline, so nothing in the effect ceiling argues for a heavier engine. The renderer stays behind `view.ts`, so this is a replaceable dependency rather than an architectural commitment.

Pixi owns only the play space; DOM elements own forms, dialogs, HUD, status, and accessibility semantics. The ground is tinted by the biome the camera stands in: `applyBiome` reads the shared world field, cross-fades the region's palette against its neighbour's by the field's own border blend, and paints the background and grid from the result — the third channel a biome's identity reaches a player through, after its materials and its terrain, and deliberately held well below the gameplay layer's contrast. The world renderer uses `Graphics` geometry for the grid, safety/world rings, actors, held weapons, projectiles, telegraphs, health bars, names, circular trees, and square walls. Damaged destructible terrain gets a compact health bar; undestroyable fixtures do not. Fill communicates class/element identity while outlines communicate self/squad/neutral/hostile allegiance, with silhouette providing the required redundant non-color channel. The shared telegraph layer renders table-driven circle, cone, line, and ring shapes beneath actors; opacity intensifies pending warnings, holds active areas, then flashes and fades resolved geometry. Because the server serves a strict `Content-Security-Policy` with `script-src 'self'` (no `unsafe-eval`), `view.ts` imports `pixi.js/unsafe-eval` for its side effect, which swaps Pixi's runtime code generation for eval-free polyfills before the renderer is created.

Desktop controls are WASD/arrows, pointer aim/fire, Shift or the secondary pointer button to scope, 1–6 or the mouse wheel to select an equipped slot, Space dash, R reload, and E interact. Scoping is the one camera exception the [HUD specification](game/ui/game-view-and-hud.md#camera) allows: a DOM vignette blacks out peripheral vision while the world camera is pushed halfway toward the aim, and `pointerWorld` compensates so aim stays relative to the body rather than to the shifted camera. A raised shield draws the arc it actually blocks, and a scoped body draws a sight line, so both committed stances are readable from outside. Touch-first layouts use fixed, independently captured movement and aim/fire sticks centered in their half-viewports, keep direct world-touch firing, place dash/interact and reload/scope above the corresponding stick, and place the six slot buttons in a safe-area-aware row above them. Source-keyed held inputs prevent one pointer release from cancelling another control. Hotbar DOM nodes survive high-frequency snapshot redraws, while hotbar/menu groups and important overlays activate directly on touch pointer-up. The fixed device-scale viewport blocks iOS gesture/double-tap/focus zoom; gameplay also disables text selection/callouts without disabling form-field editing. The HUD exposes health, class resource, the six-slot action bar with the selected slot marked by border and weight as well as fill, danger/PvP state and the biome and material grade underfoot — resolved through the shared world field, with safety and services resolved through the shared outpost overlay, rather than through a radius comparison of the client's own — latency, reconnect state, death, and respawn. The compact top-right non-modal menu leaves movement/combat controls active and reacts to authoritative progression, inventory, safety, health/resource, zone, loadout, and selected-entity changes. Updates are coalesced to an animation frame. Fine-pointer clients collapse it shortly after pointer exit and expand it on hover; touch contact never runs those hover handlers and uses the explicit minimize control. Its Loadout section is viewable anywhere and editable only inside a safe zone, where it states the lock reason rather than silently disabling controls; its World section names the region, its danger band, exactly which materials that ground can yield from the same `materialsAt` rule the server answers with, and the nearest outpost discovered this session; its Crafting section carries the ride recipes alongside weapons and ammunition. Outpost no-PvP boundaries are drawn on the ground, so the bubble a player is protected inside is seen rather than only stated. Home authentication and character creation remain modal.

## Configuration

| Environment variable | Default | Meaning |
|---|---|---|
| `SPELLFIRE_ADDRESS` | `:8080` | HTTP/WebSocket listen address |
| `SPELLFIRE_DATABASE` | `spellfire.db` | SQLite path; use `:memory:` only for one-connection tests |
| `SPELLFIRE_WEB_ROOT` | `dist` | Built frontend directory |
| `SPELLFIRE_TICK_RATE` | `tick_rate` | Authoritative ticks per second |
| `SPELLFIRE_SEND_RATE` | `send_rate` | Snapshots per second |
| `SPELLFIRE_AOI_RADIUS` | `aoi_radius` | Per-player interest half-extent on each camera axis |
| `SPELLFIRE_MAX_REWIND_MS` | `max_rewind_ms` | Maximum accepted lag-compensation age |

The last four default to the matching field in `data/tuning/simulation.json` (60, 20, 1200, and 200 as shipped), so an unset environment reproduces exactly what the client bundled. Tick and send rates must remain evenly compatible for predictable snapshot pacing; the loader rejects a table whose tick rate is not a whole multiple of its send rate. Overriding either through the environment moves the server away from the client's compiled-in prediction constants, so production should expose those values through a versioned welcome/tuning message before making them live-configurable.

## Container deployment

`Dockerfile` is a three-stage build: a `node:20-alpine` stage runs `npm ci && npm run build` to produce the client bundle, a `golang:1.22-alpine` stage compiles the server with `CGO_ENABLED=0` (the SQLite driver is pure Go, so no C toolchain is needed), and an `alpine:3.20` runtime stage carries only the static binary and the built assets. The result runs as a non-root user (`spellfire`, uid 10001) and is ~21 MB.

The client stage copies only what the shipped bundle needs — `package.json`, the lockfile, the two tsconfigs, `data`, and `web` — so `npm run build` typechecks against `tsconfig.build.json`, which is `tsconfig.json` with `web/src/**/*.test.ts` excluded. That split is load-bearing rather than tidiness: the world field's cross-language fixture lives at `testdata/worldfield.json`, outside the build context by design, and a production image has no business carrying test data in order to satisfy a typechecker. `npm run check`, which `make test` runs, still uses the full `tsconfig.json`, so tests remain strictly typechecked where the fixture actually exists.

Inside the image the binary is `/app/spellfire-server`, static assets live at `/app/web` (`SPELLFIRE_WEB_ROOT`), and the SQLite database is written to `/data/spellfire.db` (`SPELLFIRE_DATABASE`) on a mounted volume so account/character data survives container replacement. A `HEALTHCHECK` polls `/api/health`. The build stage stamps the build timestamp from the build host's clock and, if the `BUILD_COMMIT` arg is supplied, the git commit; pass it with `BUILD_COMMIT=$(git rev-parse --short HEAD) docker compose build` (the `.git` directory is not in the build context, so the commit cannot be read inside the image).

`compose.yaml` builds the image, persists `/data` in the `spellfire-data` named volume, and exposes the tuning environment variables (overridable through a sibling `.env` file). Run with `docker compose up --build -d`.

The service publishes no host port. It attaches to an external Docker network named `proxy` (create it once with `docker network create proxy`) and only `expose`s port 8080 on that network, so a reverse proxy sharing the `proxy` network reaches the server at `http://spellfire:8080`. The proxy must forward both plain HTTP (`/`, `/api/...`) and WebSocket upgrades (`/ws`) to that address. To run standalone without a proxy, add a `ports:` mapping back to the service.

## Testing and verification

Backend tests additionally cover typed entity overrides, tree destruction, undestroyable square-wall behavior, circle/AABB collision, every completed arrangement resolving to its authored weapon, incomplete and incoherent recipes, staff crystal/stave tier compatibility, cost aggregation, per-material shortfalls, the safe-zone gate, atomic refusals, gun modifiers, all-spell crystal damage/healing/cooldown scaling, the shared projectile row surviving unmutated, crafted items round-tripping through a rejoin, the bounded developer material grant, a canister deploying where it lands and expiring, a concealing cloud casting a shadow — hiding a body behind it, keeping a body half out of its rim by the any-part rule, revealing a body inside its reveal circle, letting a rim-standing body peek out, and never shadowing the viewer's own rounds — a flashbang blinding without damaging and emptying the blinded body's snapshot, a canister detonating exactly once where it lands rather than only on a direct hit, a riot shield spending its durability, breaking, dropping, and repairing while lowered, recoil walking and settling back to aim, and a thrown gadget leaving the gun's pattern alone; the Mage kit — no spell landing damage the instant the button goes down, a placed field pulsing on who stands in it and stopping when they leave, a trap springing once on the first body in and then being spent, a zone closing with its final stun, a blink covering its distance and stopping at cover, a hit arcing to the body beside the one it struck but never back onto its caster, a homing round turning toward its target no faster than its rate, a dispel stripping both sides and paying for what it takes, armor mitigating without being spent, a wall standing, blocking movement and rounds, replacing its predecessor, and expiring, a wall refused inside safety or on top of an actor without charging the cast, a shot rewound to while a wall stood being stopped by terrain that has since gone, a blink-on-hit paying out only on landing, a spell cooldown gating beyond mana and cadence, and a running lockout reaching only its own caster's snapshot as time left and leaving it the moment it expires ([spell_test.go](server/internal/game/spell_test.go)); the shipped grid reaching tier 4 in every element and pricing each tier above the one below it; an area crystal widening a field and its telegraph together without editing the shared row, and an element bias lifting only its own school; plus effective-damage ledgers, most-contribution credit, deterministic ties, shield/overkill exclusion, combat-log cursors and life resets; windup cost/delivery, locked geometry, phase transitions and death cancellation; all four validated telegraph figures; every expanded entity/input wire field; and the 64 KiB dense-snapshot guardrail. Frontend tests cover circle/AABB prediction and collision removal, a slowed step predicting the slowed speed and two slows taking the strongest, a root predicting neither step nor a spent dash, a knockback predicting nothing and cancelling the dash it interrupted, the crafting mirror — required slot layout, fitting components, result derivation, cost aggregation, shortfalls, plain-language behavior, and a crafted instance resolving onto the action bar — plus expanded wire decoding, interact encoding, movement/dash prediction, shared-table derivation, the pending/active/resolved opacity grammar including its resolution flash, a tier-4 signature validating only with the same-element company it needs, a field's concealing identity resolving from the ability that places it, and the viewer's own ability lockouts decoding off the wire.

Scale-substrate tests hold the new world: an untouched world holds nothing but its authored fixtures, a chunk generates identically however often it is evicted and in whichever order its neighbours were built, generated scatter never overlaps across a chunk edge, a chunk holding a damaged item is never evicted while a felled one never grows back, the spatial index answers collision and snapshot interest exactly as a brute-force scan of everything resident does, every radius-derived constant survives the new scale, and 50 and 100 concurrent bodies hold the 64 KiB snapshot guardrail at the shipped terrain density while a spread population keeps residency bounded per body rather than per world ([chunk_test.go](server/internal/game/chunk_test.go)).

Terrain-and-traversal tests hold the macro structure over the live generator: the median shortest on-foot path from the hub to the rim stays well above a straight radial line (~105 s median against a ~55 s line at the current scale and player speed) while no straight radial line is clear from hub to rim, no straight radial line is clear from hub to rim, the walkable space is one connected region with no sealed pocket, and every biome region is reachable from the hub ([terrain_test.go](server/internal/game/terrain_test.go)).

World-field tests hold the geography contract on both sides of the wire. In Go: the field is deterministic and a pure function of position, biomes form radial swathes so every element appears on every ring, grade follows radius and never biome, the reward curve never falls and never flattens, and a degenerate field is refused by name with the re-roll it needs ([worldfield_test.go](server/internal/worldfield/worldfield_test.go)). In the simulation: every band's PvP and safety rules resolve through the field, protection covers a shooter reaching out of safety and one reaching into it, universal stock is available wherever a player stands while aligned stock is not, and walking outward never removes an option ([worldfield_test.go](server/internal/game/worldfield_test.go)). At load: the shipped seed clears its own coverage floor with margin, the world-wide mix is close to even, every component's materials are yielded by ground that actually exists, and each biome offers something no other one does. In the browser: the field reproduces every golden sample *exactly* — not within a tolerance, because the two implementations are written to agree to the bit — and builds the same parameters from the shipped tables the fixture was made with ([worldfield.test.ts](web/src/game/worldfield.test.ts)). The fixture itself is `testdata/worldfield.json`, written by the Go suite under `SPELLFIRE_UPDATE_GOLDEN=1`.

Outpost and rideable tests hold Phase 3.3. In the simulation: a Frontier outpost's bubble is safe and protected while the ground just outside it is not, the band underfoot is still the radial field's answer, a forward outpost offers only the services it declares while the hub offers all of them, reaching an outpost unlocks it exactly once and marks it for an immediate save, a Deadlands death respawns at the nearest unlocked Frontier outpost while an undiscovered character falls back to the hub ring, exit invulnerability is granted on the crossing out, refuses damage whole, breaks on the body's own shot, and never re-arms outside safety, and every shipped outpost stands on ground terrain generation left clear ([outpost_test.go](server/internal/game/outpost_test.go)). For rides: both classes build one into the world rather than into inventory, a build is refused outside a crafting outpost and refunds nothing because it spends nothing, a second build replaces the first, a mounted step covers the ride's own speed and fires nothing, interact dismounts where the ride stands, damage lands on the ride until it is destroyed and the rider is put back on foot unhurt, a ride cannot be mounted until the combat lockout expires, a ride is reaped when its owner leaves, and both the mount and its rider's `mounted` flag reach the snapshot ([rideable_test.go](server/internal/game/rideable_test.go)). In the browser, the outpost mirror agrees with the shipped table on bubbles, services, and the nearest discovered outpost, and prediction steps a mounted body at the ride's speed, refuses to spend a dash, and still applies a slow ([outposts.test.ts](web/src/game/outposts.test.ts), [prediction.test.ts](web/src/game/prediction.test.ts)).

Line-of-sight tests separately hold the shared visibility contract: circle and box terrain occlude symmetrically only when their entity attribute enables it, trees do not occlude, destroyed terrain reveals immediately, the authored Mage wall hides the body behind it, occlusion uses the whole silhouette so a body wider than its cover peeks past it while cover wide enough shadows it whole, area fields and smoke are sent even through cover, and automatic acquisition falls through hidden bodies to the nearest visible target through both terrain and smoke. Client tests cover bounded nearest-occluder packing, circle/box shader encodings, and the veil shader's declared float precision, without which its circular occluders stop casting on fp16 drivers.

Run `make test` for Go tests, frontend tests, and strict TypeScript checking. Run `make build` to produce `dist/` and compile the Go server.

## Deliberate initial limitations

The foundation implements the requested accounts, multiplayer transport, authoritative combat/netcode, both player classes, a chunked procedural world at its full radius with a deterministic danger/biome/grade field over it, per-biome terrain archetypes funnelled through ridge-belt chokepoints, destructible collidable terrain, one fixed undestroyable wall fixture, and recipe-blueprint crafting. Outposts now exist: discoverable safe bubbles with per-row services, nearest-unlocked respawn, exit invulnerability, and crafted rideable transport, with fast travel banned outright. The following GDD systems are represented in UI/reference language but are not falsely presented as functional: harvesting/material persistence and death drops, squads and squad loot, Sentry mobs, marketplace, and world bosses. Their rules or values remain open in the source specifications, and each should be added as a separate authoritative module rather than embedded in `World`.

Other known limits are one world process and no distributed session cache. The per-client snapshot path is now a spatial-index query per family rather than a scan of the world, and terrain is chunked, so cost follows what is near a viewer rather than how large the world is. Rewind now resolves against the terrain that stood at the claimed moment — `Entity.presentAt` reads the spawn and destruction stamps every world item carries — so a stone wall raised or dropped inside the 200 ms window, and a tree felled inside it, agree with the cover the shooter saw. What is still not historical is a *surviving* item's health, which only matters for the damage a round deals to terrain rather than for whether it was stopped. The byte budget is measured and guarded and the concurrency load test is in place, but reaching the [100+ player design target](game/design/world.md) under battle density still requires snapshot deltas/compression and priority tiers before production claims are made.
